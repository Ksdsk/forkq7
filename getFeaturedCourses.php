<?php
  // include('db_connection_training.php');
  // // $sql = "SELECT * FROM course WHERE language_id='en' ORDER BY course_id DESC LIMIT 3";
  // //   $result = mysqli_query($connect, $sql);
  // //   $return = array(array());
  // //   $index=0;
  // //   while($row = mysqli_fetch_assoc($result)) {
  // //     $return[$index]['course_name'] = $row['course_name'];
  // //     $return[$index++]['course_description'] = $row['course_description'];
  // //   } 
  // //   // header('Content-type: application/json');
  // //   echo json_encode($return);

  // if(isset($_GET['lang'])) {
  //   $sql = "SELECT * FROM course WHERE language_id='".$_GET['lang']."' ORDER BY course_id DESC LIMIT 3";
  //   $result = mysqli_query($connect, $sql);
  //   $return = array(array());
  //   $index=0;
  //   while($row = mysqli_fetch_assoc($result)) {
  //     $return[$index]['course_name'] = $row['course_name'];
  //     $return[$index++]['course_description'] = $row['course_description'];
  //   } 
  //   header('Content-type: application/json');
  //   echo json_encode($return);
  // }
  echo '["a","b","c"]';
?>